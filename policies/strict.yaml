name: strict
wip:
  max_candidate_per_organ: 2
  max_public_process_per_organ: 1
validation:
  strict_default: true
  fail_on_warnings: true
routing:
  max_path_depth: 5
  max_paths_returned: 5
observability:
  enabled: true
  top_failure_buckets: 20
